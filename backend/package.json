{
  "scripts": {
    "dev": "cd weather_mpei && python manage.py runserver --insecure",
    "install": "py -m venv venv && venv\\Scripts\\activate.bat && cd weather_mpei && pip install -r requirements.txt"
  }
}
