{
  "scripts": {
    "dev": "cd weather_mpei && python manage.py runserver --insecure",
    "install": "py -m venv venv && cd weather_mpei && pip install -r requirements.txt",
    "venv": "venv\\Scripts\\activate.ps1"
  }
}
